import React from 'react';
import { Container } from 'semantic-ui-react';

export default function TransactionGUI() {
    return (
        <Container style={{marginTop: '7em'}}>
            <h1>Transaction GUI</h1>
        </Container>
    );
}