import React from 'react';
import { Container } from 'semantic-ui-react';

export default function SaleScrollDisplay() {
    return (
        <Container style={{marginTop: '7em'}}>
            <h1>Sale Scroll Display</h1>
        </Container>
    );
}